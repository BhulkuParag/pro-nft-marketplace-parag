import React from 'react';

type Props = {
  maxTicksLimit: number;
};

const MarketCapAndVolume: React.FC<Props> = ({ maxTicksLimit }) => {
  return (
    <div>
      {/* Render chart with maxTicksLimit */}
    </div>
  );
};

export default MarketCapAndVolume;
