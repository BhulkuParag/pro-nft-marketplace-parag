import MarketCapAndVolume from "../../../Components/charts/nft/MarketCapAndVolume";

const MarketCapAndVolumeIndex = () => {
  return <MarketCapAndVolume maxTicksLimit={10} />; // Provide a suitable value for maxTicksLimit
};

export default MarketCapAndVolumeIndex;
